using System;
using HoldMyBeerServer.Dtos.Friends;
using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Builder;
using Microsoft.AspNetCore.Routing;
using HoldMyBeerServer.Dtos;


namespace HoldMyBeerServer.Endpoints;

public static class FriendsEndpoints
{

// private static readonly List<FriendsDto> friends = new();

// const string GetFriendsEndpointName = "GetFriends";

// public static RouteGroupBuilder MapFriendsEndpoints(this WebApplication app){

// var group = app.MapGroup("friends").WithParameterValidation();


// group.MapPost("/request",(int RequesterId, int AddresseeId)=>{
    
//  bool? addresseeId = users.Find(user=>user.Id == AddresseeId);

// });

// }

}
